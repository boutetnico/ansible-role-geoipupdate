---
dependency:
  name: galaxy

driver:
  name: docker

lint:
  name: yamllint

provisioner:
  name: ansible
  playbooks:
    converge: ../shared/playbook.yml
  lint:
    name: ansible-lint
  ansible_args:
    - -e geoipupdate_account_id:${MAXMIND_ACCOUNT}

verifier:
  name: testinfra
  directory: ../shared/tests/
  lint:
    name: flake8
